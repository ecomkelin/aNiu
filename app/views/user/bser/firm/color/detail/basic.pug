table.mt-3.table.border
	tr
		td: strong #{Lang.code}:
		td #{object.code}
	tr
		td: strong #{Lang.name}:
		td #{object.nome}
	tr.detail
		td: strong Tel:
		td #{object.tel}
	tr.detail
		td: strong P.IVA:
		td #{object.iva}
	tr.detail
		td: strong #{Lang.cf}:
		td #{object.cf}
	tr.detail
		td: strong Addr.:
		td #{object.addr}
	tr.detail
		td: strong #{Lang.zip}:
		td #{object.post}
	tr.detail
		td: strong #{Lang.city}:
		td #{object.city}
	tr.detail
		td: strong #{Lang.docu}:
		td #{object.doct}
